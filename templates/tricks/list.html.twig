{% extends 'base.html.twig' %}

{% block title %}Figures{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="bg-light p-3 py-4 h-100">
            <h1>Toutes les figures</h1>

            {% if trickGroup %}
                <h5 class="mt-3"> Catégorie: <strong class="text-decoration-underline">{{ trickGroup.name }}</strong></h5>
            {% endif %}
        </div>
        <div class="tricks-container py-5">
            {% for trick in tricks %}
                {% include 'tricks/_trick.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}